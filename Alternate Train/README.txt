These scenes were used for experimenting with how to make the train.

There are a few different implementations of the train car movement in new_train.gd
that make the train move in different ways

The implementation that should be used is train.gd
